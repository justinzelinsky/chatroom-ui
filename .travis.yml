language: node_js
node_js:
  - '15.3.0'
os:
  - osx
install:
  - npm ci
jobs:
  include:
    - script: npm run lint
    - script: npm run tracker
    - name: test
      post_install:
        - npm i -g codecov
      script:
          - npm test
          - codecov